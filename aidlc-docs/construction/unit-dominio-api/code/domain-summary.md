# Domain summary — unit-dominio-api

Gerado: entidade `Lote`, `StatusLote`, excecoes tipadas, portas e esboco de validadores em `libs/src/lote_shared/`.
PBT P-API-01..04 em `libs/tests/test_lote_pbt.py`.
