# API summary — unit-dominio-api

FastAPI em `lote_api/presentation/app.py` + `main.py`.
Rotas: POST/GET/PUT/DELETE `/lotes`, `GET /health`, middleware `X-Request-ID`.
Testes TestClient: `api/tests/test_api.py`.
